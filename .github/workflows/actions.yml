name: action workflow

on: [push]

jobs:
 run-github-actions:
   runs-on: ubuntu-latest
   steps:
     - name: List files
       run: |
        pwd
        ls -a
     - name: Checkout
       uses: actions/checkout  
     - name: List files ofter checkout
       run: |
        pwd
        ls -a
     - name: simple javascript actions
       uses: actions/hello-world-javascript-action@v1
       with:
        who-to-greet: DevOps hint 

   
